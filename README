ScalaGranny
===========

Run this app with Play 2.1 using an H2 in-memory database:

play ~run


Run this app with Play 2.1 using a Postgres database:

play -Dconfig.file=conf/local-postgres.conf ~run